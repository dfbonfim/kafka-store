version: '3'
services:
    api:
      build: api/.
      links:
        - mysql
    mysql:
      image: mysql
      environment:
        MYSQL_ROOT_PASSWORD: root
      volumes:
        - ./scripts/mysql/initdb.sql:/docker-entrypoint-initdb.d/initdb.sql